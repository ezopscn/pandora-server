####################################################################################
# 系统配置
####################################################################################
system:
  # 监听地址，服务绑定 IP 地址，服务启动时可以使用 --listen 参数覆盖该值
  listen: 0.0.0.0
  # 监听端口，推荐使用 3000-65535 之间的端口，服务启动时可以使用 --port 参数覆盖该值
  port: 12345